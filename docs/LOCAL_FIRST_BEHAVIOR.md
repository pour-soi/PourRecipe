# 本地优先行为

IndexedDB 是交互路径的第一写入点。未登录、离线或 Supabase 暂停时，仍可创建/编辑/搜索食谱，上传图片，执行已缓存语言的 OCR，编辑 OCR，管理分类标签和制作记录，删除恢复，以及导出/导入 ZIP。

云端失败时界面显示“已保存在本机，等待云端同步。”图片仍使用本地 Blob，并保留待同步任务。恢复连接后队列继续；旧 revision 进入冲突界面，不以时间戳静默覆盖。

下载云端数据前必须先生成 ZIP。暂不同步不会清空队列或本地数据。
