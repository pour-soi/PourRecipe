# ZIP 恢复验证

自动化测试构造三份中/英/混合食谱、分类标签、三条制作记录、最佳版本、五张不同用途图片、OCR 原始/编辑文字及回收站食谱。流程为导出、校验、删除数据库、全新导入、逐项比较、再次导出并比较逻辑数量。

损坏包覆盖：非 ZIP、空包、超大包、缺 manifest、不支持版本、JSON/哈希损坏、缺图、路径穿越、重复 UUID/无效关联、异常压缩比和事务中途失败。任何预检失败发生在写事务前；事务失败完整回滚。替换模式先生成当前数据 ZIP，合并模式对相同 UUID 建冲突且不覆盖本机原文。
